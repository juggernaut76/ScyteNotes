<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="notes.scyte.eu.scytenotes"
          android:versionCode="1"
          android:versionName="Version 0.1">

    <uses-sdk
            android:minSdkVersion="14"
            android:targetSdkVersion="17" />

    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name="notes.scyte.eu.scytenotes.MainActivity"
                  android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity android:name=".NoteTextActivity"
                  android:windowSoftInputMode="stateVisible"/>
        <activity android:name=".NoteSketchActivity">
        </activity>
        <activity android:name=".NoteVoiceActivity">
        </activity>
    </application>

</manifest>
